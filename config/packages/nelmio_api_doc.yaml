nelmio_api_doc:
    documentation:
        info:
            title: Api
            description: Api managing companies and its employees
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
    models:
        use_jms: false
    use_validation_groups: true